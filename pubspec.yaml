name: clock_app
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  build_runner: 2.4.13
  flutter:
    sdk: flutter
  flutter_bloc: 9.1.0
  equatable: 2.0.7
  get_it: 8.0.3
  injectable: 2.5.0
  injectable_generator: 2.5.0
  intl: 0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

